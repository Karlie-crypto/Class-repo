#include <stdio.h>
/**
 * main - main entry
 * Description: converting the characters to numbers
 * Return: Always 0 success
*/
int main(void)
{
char c;
printf(" Enter a character: ");
scanf("%c", &c);
printf(" ASCII value of %c is %d\n", c,c);

return (0);    
}