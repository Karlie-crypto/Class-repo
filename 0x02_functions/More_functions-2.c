#include <stdio.h>
/**
 * Main - main entry
 * Description: A program with function arguments and returning the  value
 * Return: Always 0 success
 */
int arg_2datatype(char a);
{
  int Mark;
  switch (a)
  case 'A': Mark = 98;
  
  case 'B': Mark = 80;
    return Mark;
}
