#include <stdio.h>
/**
 * main - main entry
 * Description: checking the statement if it is false
 * Return 0 Always success
 */
int main(void)
{
  int a, b;
  if (a < b && a != b)
    {
      printf("It is true ");
    }
  else
    {
      printf("It is false ");
    }

  return (0);

} 
