#include <stdio.h>
/**
 * main - main block
 * Description: Addition of two integers
 * Return: Always 0 success
 */

int main(void)
{
int num1 = 5, num2 = 3, addition = num1 + num2;

printf(" %d + %d = %d\n", num1, num2, addition);

return (0);
}
